import { BtnStrokedDirective } from './btn-stroked.directive';

describe('BtnStrokedDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnStrokedDirective();
    expect(directive).toBeTruthy();
  });
});
