import { BtnFilledDirective } from './btn-filled.directive';

describe('BtnFilledDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnFilledDirective();
    expect(directive).toBeTruthy();
  });
});
